declare module '@openhealthnz-credentials/pdf-image-qr-scanner' {
  // Declare o que você sabe sobre o módulo aqui.
  // Por exemplo, se você sabe que o módulo exporta uma função `scanFile`, declare-a assim:

  export function scanFile(file: any): Promise<string | null>
}
